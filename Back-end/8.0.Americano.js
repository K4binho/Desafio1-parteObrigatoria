numeros = [1, 3, 2, 1,5]
let somador = 0;
let posicaoMais = numeros[1];

for (let i = 0; i < numeros.length; i++) {//somador, soma a dor, a dor ele soma, a dor sem receio, teu cu na reta, meu pau sem freio
    somador += numeros[i];
}   

console.log(posicao);